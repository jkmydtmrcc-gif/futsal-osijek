# Supabase → Project Settings → API
# Kopiraj u .env (lokalno) i u Vercel → Settings → Environment Variables.
# Bez ovih vrijednosti stranica radi na ugrađenom sadržaju iz src/data/site.js.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
